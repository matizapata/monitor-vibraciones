# Copia este archivo como .env para correr el backend localmente.
# En Railway estas variables se configuran en el panel "Variables".

DATABASE_URL=postgres://usuario:password@localhost:5432/vibraciones
API_KEY=cambiar-esta-clave
ALERT_THRESHOLD=2.0
# PORT lo asigna Railway automaticamente; en local usa 3000 por defecto.
